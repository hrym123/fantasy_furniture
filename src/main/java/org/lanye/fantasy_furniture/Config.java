package org.lanye.fantasy_furniture;

/**
 * 预留：需要模组配置文件时，在此定义 {@link net.minecraftforge.common.ForgeConfigSpec} 并在主类中
 * {@code ModLoadingContext.get().registerConfig(...)} 注册。
 */
public final class Config {
    private Config() {}
}
